/**
 * Public source repo, linked from the footer and the unsupported-device notice.
 * Client-only, so it lives here rather than in #shared.
 */
export const REPO_URL = 'https://github.com/MartinBorak/misbutton'
